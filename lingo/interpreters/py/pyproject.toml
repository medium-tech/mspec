[build-system]
requires = ['setuptools>=61.0']
build-backend = 'setuptools.build_meta'

[project]
name = 'lingolib'
version = '0.1.0'
description = 'Lingo Python interpreter bootstrap package'
requires-python = '>=3.9'

[project.scripts]
lingolib = 'lingolib.__main__:main'
