lingo:
    spec: 'super'
    version: '0.1.0b'

meta:
    description: 'Hello world for super spec with multiple sub-specs.'

specs:
    - lingo-stdlib.yaml
    - hello-library.yaml
    - hello-data.yaml
    - hello-frontend.yaml
    - hello-backend.yaml

run:
    spec: 'hello-frontend.yaml'
    params:
        person_id: '1'
