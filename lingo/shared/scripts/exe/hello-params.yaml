lingo:
    spec: 'exe'
    version: '0.1.0b'

meta:
    description: 'Hello world for user input params (bool, int, str).'

params:
    is_enabled:
        define: bool
        default: false

    count:
        define: int
        default: 0

    name:
        define: str

main:
    join:
        separator: '\n'
        items:
            - concat:
                - 'is_enabled: '
                - str:
                    get: params.is_enabled

            - concat:
                - 'count plus 1: '
                - str:
                    add:
                        a:
                            get: params.count
                        b: 1

            - concat:
                - 'hello, '
                - get: params.name
