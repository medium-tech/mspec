lingo:
    spec: 'exe'
    version: '0.1.0b'

meta:
    description: 'Hello world for user input params (bool, int, str).'

params:
    is_enabled:
        define: bool
        default: false
        description: 'A test boolean value'

    count:
        define: int
        default: 0
        description: 'A test integer value'

    name:
        define: str
        description: 'The name of the person to greet'

main:
    join:
        separator: '\n'
        items:
            - concat:
                - 'is_enabled: '
                - str:
                    get: params.is_enabled

            - concat:
                - 'count plus 1: '
                - str:
                    add:
                        a:
                            get: params.count
                        b: 1

            - concat:
                - 'hello, '
                - get: params.name
