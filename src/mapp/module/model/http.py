def http_model_create(model_class:type, data:dict):
    print('[PLACEHOLDER] Would create a single model via HTTP API.')

def http_model_read(args):
    print('[PLACEHOLDER] Would read a single model via HTTP API.')

def http_model_update(args):
    print('[PLACEHOLDER] Would update a single model via HTTP API.')

def http_model_delete(args):
    print('[PLACEHOLDER] Would delete a single model via HTTP API.')

def http_model_list(args):
    print('[PLACEHOLDER] Would list models via HTTP API with optional pagination.')
