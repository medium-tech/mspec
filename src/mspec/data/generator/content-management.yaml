lingo:
  version: 'generator-beta-1'
  
project:
  
  name:
    lower_case: 'content management'

  description: 'A test project for a simple content management system'

  author:
    name: 'B rad C'
    email: 'sample@email.com'

server:
  port: 2002

client:
  default_host: 'http://localhost:2002'

modules:
  content_management:
    name:
      lower_case: 'content management'

    models:
      content:
        auth:
          require_login: true
        
        fields:
          user_id:
            name:
              lower_case: 'user id'
            type: foreign_key
            references:
              module: 'auth'
              table: 'user'
              field: 'id'
            examples:
              - "1"
              - "2"
              - "3"
          name:
            name:
              lower_case: 'name'
            type: str
            examples:
              - "My First Post"
              - "A Day in the Life"
              - "How to Use Mapp"